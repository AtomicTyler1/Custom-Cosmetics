<Project>
  <PropertyGroup>
    <!-- Set if the default steam game install directory doesn't work. -->
    <!-- <CrashoutCrewGameRootDir></CrashoutCrewGameRootDir> -->

    <!-- Set if your BepInEx/ directory isn't in the game files. -->
    <!-- <CrashoutCrewBepInExDir></CrashoutCrewBepInExDir> -->

    <!-- Enable/disable deploying mod files to BepInEx directory. -->
    <DeployModFilesEnabled>true</DeployModFilesEnabled>
  </PropertyGroup>
</Project>
